# HANDOFF

Last updated: 2026-05-06. Matrix-supported fresh runs default to `core-v2`; Review default, Fix default/lite/deep/autonomous, Build default/lite/deep/autonomous, Explore default/lite/deep/autonomous/tournament, Migrate default/deep/autonomous, and Sweep default/lite/deep/autonomous now route through core-v2 by default. Phase 5.12 proves retained/v1 checkpoint folders still project and resume through retained compatibility. Phase 5.13 moves shared registry and catalog derivation ownership to `src/flows/**`, while old `src/runtime/**` registry paths remain compatibility re-exports. Phase 5.14 adds `src/compat/retained-runtime.ts` as the facade for retained fresh-run fallback, retained/v1 checkpoint resume, retained snapshot derivation, retained trace reading, and retained trace reduction. Phase 5.15 starts the old runner/handler oracle-test mapping lane by moving accidental test-only relay type imports to shared/facade paths, adding a production import guard for retained execution modules, splitting more retained runner/direct handler tests so old runner imports are value-level execution/helper calls, and adding core-v2 twins for terminal outcome mapping, rich checkpoint route labels, bounded retry loops, relay verdict admission, connector identity, and connector provenance. Phase 5.16 continues that lane with core-v2 twins for relay recovery routes, canonical report gating, and connector invocation failure recovery/abort behavior. Phase 5.17 adds strict v2 final-result proof for executor throws and pass-route cycles. Phase 5.18 hardens public compatibility policy without changing runtime defaults: runtime reasons no longer imply a future v2 `composeWriter` hook, CLI usage names retained compatibility paths, and `circuit-next create` summaries tell operators that custom flow roots stay retained by default. Phase 5.19 moves retained execution test calls through the compatibility facade: tests no longer import `runCompiledFlow` or `resumeCompiledFlowCheckpoint` directly from `src/runtime/runner.js`. Phase 5.20 exposes retained helper calls through the same facade; direct old runner test imports are now limited to the explicit `writeComposeReport` public-path compatibility proof in `tests/runner/fix-report-writer.test.ts`. Phase 5.21 adds `src/compat/retained-checkpoint-folders.ts` as the narrower boundary for retained/v1 checkpoint resume, retained snapshot derivation, retained trace reading, and retained trace reduction; CLI resume, handoff, and run-status now import saved-folder support from that boundary. Phase 5.22 centralizes public compatibility policy strings in `src/cli/runtime-compatibility-policy.ts` so runtime reasons, CLI usage, and custom-flow summaries share one source without changing behavior. Phase 5.23 narrows remaining retained checkpoint-resume test imports to `src/compat/retained-checkpoint-folders.ts`. Phase 5.24 adds core-v2 twins for verification pre-write failures and makes core-v2 emit `check.evaluated` failure evidence without writing the canonical verification report. Phase 5.25 adds test-only core-v2 twins for sub-run failure evidence: missing resolvers, wrong child flow ids, child runner throws, and missing child verdicts. Phase 5.26 adds test-only core-v2 twins for checkpoint auto-resolution failures when safe default or safe autonomous choices are missing. Phase 5.27 formalizes `composeWriter` as retained-runtime-only compatibility with an explicit policy constant and CLI tests proving `v2Executors` cannot bypass that policy. Phase 5.28 adds test-only core-v2 twins for relay result shape edge cases: array bodies, `null` bodies, and empty verdict strings. Phase 5.29 hardens core-v2 sub-run failure evidence for resolver throws and malformed child result files. Phase 5.30 hardens core-v2 sub-run resolver/admission behavior: invalid child compiled-flow bytes now produce check failure evidence, an allowed child verdict is not admitted unless the child run closes as `complete`, and the final result writer ignores non-complete sub-run verdict traces even when older/custom traces lack admission metadata. Phase 5.31 adds a test-only core-v2 twin for dynamic fanout expansion failures before `fanout.started`. Phase 5.32 moves connector subprocess modules and relay materialization to neutral `src/connectors/**` ownership, with old `src/runtime/connectors/**` paths kept as compatibility re-exports. Phase 5.33 moves the natural-language router and schematic compiler to neutral `src/flows/router.ts` and `src/flows/compile-schematic-to-flow.ts`, with old `src/runtime/router.ts` and `src/runtime/compile-schematic-to-flow.ts` kept as compatibility re-exports. Phase 5.34 reviewed retained trace/status/progress/checkpoint-state ownership and approved no implementation move; it adds only behavior-preserving guardrails around the existing retained checkpoint-folder facade and intentional old-oracle imports. Phase 5.35 moves retained runtime imports of already-neutral shared helpers to `src/shared/**` owners and guards against production imports of old helper wrapper paths; old wrapper files remain compatibility surfaces. Phase 5.36 adds a core-v2 terminal verdict derivation twin proving the later admitted relay verdict wins in final results. Phase 5.37 moves pure terminal verdict derivation to `src/shared/terminal-verdict.ts`, keeps `src/runtime/terminal-verdict.ts` as a compatibility re-export, points retained result finalization at the neutral helper, and guards production code away from the old wrapper path. Phase 5.38 moves fanout join-policy evaluation to `src/shared/fanout-join-policy.ts`, points retained and core-v2 fanout at the shared helper, and keeps the old retained fanout path as a compatibility re-export. Phase 5.39 moves recovery route priority to `src/shared/recovery-route.ts`, points retained relay/verification and core-v2 production relay/verification at the shared helper, and keeps the old retained route helper path as a compatibility re-export. Phase 5.40 moves the generic JSON report helper to `src/shared/json-report.ts`, points retained result finalization and retained handlers at the shared helper, and keeps the old retained handler helper path as a compatibility re-export. Phase 5.41 moves fanout aggregate report body construction to `src/shared/fanout-aggregate-report.ts`, points retained and core-v2 fanout aggregate writing at the shared helper, and keeps the old retained fanout aggregate helper path as a compatibility re-export. Phase 5.42 centralizes the no-verdict sentinel on `src/shared/relay-support.ts`, with retained/core-v2 sub-run and fanout using or re-exporting the shared constant. Phase 5.43 moves pure fanout branch-template helpers to `src/shared/fanout-branch-template.ts`, with retained and core-v2 branch expansion keeping their own output shapes. Phase 5.44 hardens core-v2 terminal verdict parity: non-complete final results now omit admitted verdicts, matching retained/shared terminal verdict semantics, and the fanout join-policy old-path proof now targets the wrapper file directly. Phase 5.45 adds direct old-path compatibility assertions for shared helper wrappers such as config loading, manifest snapshots, operator summaries, flow-kind policy, relay support, run-relative paths, selection resolution, and write-capable worker disclosure. Phase 5.46 adds v2 fanout branch-level failure twins for worktree provisioning throws and child runner throws, proving failed branches record `<no-verdict>`, sibling branches can finish under `continue-others`, and disjoint-merge aborts with failure evidence. Phase 5.47 adds a v2 disjoint-merge file-conflict twin proving completed branches that touch the same file fail the join while worktrees still clean up. Phase 5.48 adds a v2 disjoint-merge changed-file discovery failure twin proving completed branches fail with `file-disjoint validation failed` when changed-file evidence collection throws, while worktrees still clean up. Phase 5.49 adds v2 fanout join-policy executor twins for pick-winner success/failure, aggregate-only failure, and resolver throws, proving the shared join-policy decisions are carried through trace evidence, aggregate reports, and final outcomes. Phase 5.50 adds a v2 sub-run report-path guard twin proving divergent `writes.report` and `writes.result` abort before child start with check failure evidence. Phase 5.51 adds v2 fanout/sub-run twins for aggregate-only success with non-admitted branch verdicts, abort-all short-circuiting under bounded fanout concurrency, and missing child-runner preflight failure before `sub_run.started`. Phase 5.52 adds v2 relay transcript sequence twins for admitted checks, failed checks, and connector throws, proving durable relay evidence ordering without changing relay behavior. Phase 5.53 adds a v2 fanout trace-sequence twin proving successful sub-run fanout records branch lifecycle, aggregate writing, join evidence, check pass, step completion, and run closure in order. Phase 5.54 centralizes public compatibility routing policy helpers in `src/cli/runtime-compatibility-policy.ts`, leaving behavior unchanged while adding direct policy tests and the strict-v2 plus rollback plus `composeWriter` fail-closed regression. Phase 5.55 adds `src/compat/public-runtime-paths.ts` as a machine-checkable manifest for every `src/runtime/**/*.ts` path, categorizing wrappers versus retained-owned files and wiring guard tests to that manifest without deleting or deprecating any old path. Phase 5.56 moves retained runtime internal registry imports to the neutral `src/flows/registries/**` owners and strengthens wrapper import guards to use the public runtime path manifest for old absolute and relative wrapper imports.

Phase 5.57 moves retained handler/test imports of the pure result path helper to `src/shared/result-path.ts`, while `src/runtime/result-writer.ts` remains the retained writer and old result-path compatibility surface. Phase 5.58 adds soft-deprecation metadata for the approved low-risk shared-helper and flow-authoring old import paths, plus a checked policy note and draft release-note wording; no wrapper is deleted, no package export changes, and no import-time warning is added.

The repo is intentionally dirty with Phase 4.37 onward migration work, refreshed golden examples, and review packets. The retained runtime still owns arbitrary fixtures, custom flow roots, rollback, retained/v1 checkpoint folders, public `composeWriter` compatibility, old oracle tests, and retained trace/progress/checkpoint/status behavior. Connector subprocesses/materializer now live in `src/connectors/**`; router/compiler implementations now live in `src/flows/**`; terminal verdict derivation, fanout join-policy evaluation, recovery route priority, JSON report writing, fanout aggregate report bodies, fanout branch-template helpers, and the no-verdict sentinel now live in `src/shared/**`; the old runtime paths for those clusters remain public compatibility wrappers. Retained trace reader/writer, reducer, snapshot, append-and-derive, progress projector, checkpoint resume, and checkpoint handler code stay retained-owned for now. Release proof no longer imports `dist/runtime/runner.js` or passes public `composeWriter`; it uses v2 executor injection. Do not start old runtime deletion yet. Continue behavior-preserving cleanup or v2/shared oracle twins autonomously. Require a new review package before any further deprecation category, import-time warning, fail-closed behavior, selector widening, saved-folder policy change, connector behavior change, trace/status/checkpoint ownership move, wrapper deletion, or old runtime deletion.
