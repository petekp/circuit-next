# HANDOFF

Last updated: 2026-05-07. Circuit now treats `src/runtime/` as the normal engine foundation. Fresh runs, checkpoint resume, run status, handoff continuity, connector relay execution, sub-runs, and fanout all use the same runtime run-folder contract: a valid manifest snapshot plus a matching runtime bootstrap trace.

Next group: none pending from the runtime foundation cleanup. The full verification sequence passed after active docs, generated surfaces, runtime paths, CLI policy, status projection, and guardrails were aligned to `src/runtime/`. Preserve unrelated dirty work in the repo.
