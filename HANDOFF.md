# HANDOFF

Last updated: 2026-05-05. The core-v2 migration has completed the low-risk retained-runtime narrowing lane. Matrix-supported fresh runs default to `core-v2`; checkpoint resume, checkpoint-waiting depths, unsupported modes, arbitrary fixtures, rollback, and programmatic `composeWriter` remain retained-runtime-owned. Phase 4.37 extracted retained checkpoint resume preparation to `src/runtime/checkpoint-resume.ts`, and Phase 4.41 extracted terminal verdict derivation to `src/runtime/terminal-verdict.ts`; `npm run verify` passed after the Phase 4.41 changes.

The repo is intentionally dirty with the Phase 4.37-4.41 migration work and review packets. The remaining migration work is no longer cheap cleanup: connector subprocesses, relay materialization, registries, trace/progress/reducer/snapshot internals, checkpoint resume ownership, and old runner/handler deletion all need an explicit product or architecture decision before code moves. Next: choose whether to keep checkpoint resume permanently retained, implement v2 resume parity, or start a separate reviewed ownership plan for registries/connectors/materialization.
