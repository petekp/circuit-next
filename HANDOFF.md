# HANDOFF

Last updated: 2026-05-07. The retained-runtime compatibility posture is superseded. There are zero external users, so the v2 work is in final cutover mode instead of external review-packet mode. Retained and v1 run folders fail closed with “This run folder was created by the retired runtime. Start a fresh run.” Old runner, checkpoint, progress, result-writer, and checkpoint-handler entrypoints now fail closed, and the old handler, trace, reducer, snapshot, relay-selection, retained compatibility facade, and v1 run-status projector implementation files have been removed.

Next group: choose the next remaining old `src/runtime/**` wrapper category. The old flow-authoring wrappers are gone; shared-helper wrappers are the likely next candidate, but they still have direct compatibility tests plus stale spec/doc references to clean up. Keep the numbered checkpoint docs compressed into `docs/architecture/v2-checkpoint-history.md`; do not recreate review packets unless a genuinely new ambiguity appears. Preserve unrelated dirty work in the repo.
