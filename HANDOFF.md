# HANDOFF

Last updated: 2026-05-06. The retained-runtime compatibility posture is superseded. There are zero external users, so the v2 work should move toward final cutover instead of preparing more external review packets. Do not continue the Phase 5.60 review-packet path unless a genuinely new ambiguity appears. Group 2 makes retained and v1 run folders fail closed instead of adapting them, and Group 3 removes the unused retained/v1 run-status projector.

Next group: doc compression. Update the historical checkpoint docs in one deliberate pass, and preserve unrelated dirty work in the repo.
