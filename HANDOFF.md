# HANDOFF

Last updated: 2026-04-29. We are pushing Circuit toward public release readiness with original-Circuit parity as the target, not a narrowed alpha. The current track is release-truth infrastructure plus high-confidence follow-through: checked parity snapshots, generated current capability snapshots, public claim checking, proof coverage, stale-claim audits, and readiness reports. The latest important behavior change is that `decide:` now routes to a real Explore tournament entry mode with Decision-stage option drafting, relay fanout, stress review, a bounded tradeoff checkpoint, resume, and final decision/result reports.

Recent work landed the release infrastructure, several review fixes, and the Explore tournament implementation. `router:intent:decide` is now implemented in generated release truth, while `proof:explore-decision` remains a release blocker until a golden run is captured. Full `npm run verify` passed after the latest changes. The repo is still intentionally dirty with readiness work. Next: capture the Explore Tournament golden proof, then continue the highest-risk trust blockers from `docs/public-release-readiness.md` and `docs/release/readiness-report.generated.md`: rich route execution or honest blocking, custom connector protocol/docs alignment, broader proof artifact coverage, and human-first operator output.
