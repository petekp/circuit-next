node_modules/
dist/
# Anchored: only matches <repo>/build/, not any nested path containing
# 'build/'. Without the leading slash this would shadow legitimate
# directories like src/workflows/build/.
/build/
.DS_Store
*.log
.env
.env.local
coverage/
.vitest-cache/
.turbo/
.circuit-next/
.claude/projects/
# Runtime lock from the Claude Code harness (sessionId/pid/acquiredAt). Not
# a deliverable; per-machine ephemeral state.
.claude/scheduled_tasks.lock

# Tool-run state for the Circuit harness used by this project. Ephemeral
# per-machine state; nothing here is a deliverable.
.circuit/
