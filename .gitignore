node_modules/
dist/
build/
.DS_Store
*.log
.env
.env.local
coverage/
.vitest-cache/
.turbo/
.circuit-next/
.claude/projects/
# Runtime lock from the Claude Code harness (sessionId/pid/acquiredAt). Not
# a deliverable; per-machine ephemeral state.
.claude/scheduled_tasks.lock

# Tool-run state for the Circuit harness used to build circuit-next.
# Per ADR-0002, orchestration artifacts live in the outer workspace, not in
# the circuit-next repo. The one pre-discipline run already tracked
# (phase-1-step-contract-authorship) is preserved as historical audit trail.
.circuit/
!.circuit/circuit-runs/phase-1-step-contract-authorship/
